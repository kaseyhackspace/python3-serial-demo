1. Create virtualenv:
    ````bash
    pip install virtualenv
    virtualenv env
    ```

2. enter virtualenv:
    ```bash
    source env/bin/activate
    ```
3. install `pyserial`
   ```bash
    pip install pyserial
   ```

4. 
